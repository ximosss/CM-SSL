seed: 42

data:
  path: 
  split_ratios: [0.9, 0.05, 0.05] # [train, val, test]
  split_seed: 42
  batch_size: 128
  num_workers: 16

model:
  in_channels: 1
  base_filters: 32
  kernel_size: 3
  stride: 2
  n_block: 16
  hidden_dim: 512
  projection_dim: 512

training:
  logs_dir: 
  checkpoints_dir: 
  epochs: 512
  optimizer:
    lr: 3e-4
    weight_decay: 1e-5
  loss:
    temperature: 0.07
  early_stopping:
    patience: 10
